{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run fastapi dev main.py --host 0.0.0.0 --port 8000",
    "build": "uv sync --frozen",
    "start": "uv run fastapi run main.py --host 0.0.0.0 --port 8000",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  }
}