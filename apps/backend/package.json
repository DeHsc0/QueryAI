{
  "name": "@repo/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run fastapi dev main.py --host 127.0.0.1 --port 8000",
    "build": "uv sync --frozen",
    "start": "uv run fastapi run main.py --host 127.0.0.1 --port 8000",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  }
}